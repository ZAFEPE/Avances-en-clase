info:
  name: Prueba del proyecto
  type: http
  seq: 1
  tags:
    - Sales.API

http:
  method: POST
  url: http://localhost:5211/api/categories
  body:
    type: json
    data: |-
      {
        "name": "Vegetales",
        "description": "",
        "id": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/weatherforecast"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
